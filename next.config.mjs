/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.kingsporttn.gov",
      "oaidalleapiprodscus.blob.core.windows.net",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
